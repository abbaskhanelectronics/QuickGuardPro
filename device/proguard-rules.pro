# Firebase, Compose, WorkManager and Play Services ship their own consumer rules.
# Manifest components (DeviceAdminReceiver, services, activities) are kept automatically.
-keepattributes Signature,*Annotation*
